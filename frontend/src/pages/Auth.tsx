import { useState } from 'react'; import { post } from '../api'; import type { User } from '../types';
export function LoginPage({onLogin,onRegister}:{onLogin:(u:User)=>void;onRegister:()=>void}){const [email,setEmail]=useState('admin@example.com'),[password,setPassword]=useState('Admin12345'),[error,setError]=useState('');return <div className="auth"><form className="card" onSubmit={async e=>{e.preventDefault();try{const x=await post<{token:string;user:User}>('/auth/login',{email,password});localStorage.setItem('token',x.token);onLogin(x.user)}catch(err){setError((err as Error).message)}}}><h1>เข้าสู่ระบบ</h1><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password"/>{error&&<p className="error">{error}</p>}<button className="primary">Login</button><button type="button" className="link" onClick={onRegister}>สมัครสมาชิก</button></form></div>}
export function RegisterPage({onLogin}:{onLogin:()=>void}){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[role,setRole]=useState<'STUDENT'|'TEACHER'>('STUDENT'),[message,setMessage]=useState('');return <div className="auth"><form className="card" onSubmit={async e=>{e.preventDefault();try{await post('/auth/register',{name,email,password,role});setMessage('สมัครสำเร็จ');setTimeout(onLogin,600)}catch(err){setMessage((err as Error).message)}}}><h1>สมัครสมาชิก</h1><input value={name} onChange={e=>setName(e.target.value)} placeholder="ชื่อ-นามสกุล" required/><select value={role} onChange={e=>setRole(e.target.value as 'STUDENT'|'TEACHER')}><option value="STUDENT">นักศึกษา</option><option value="TEACHER">อาจารย์</option></select><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password" required/>{message&&<p>{message}</p>}<button className="primary">Register</button></form></div>}
