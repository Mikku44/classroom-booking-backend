import type { Booking } from '../types';
export function ConfirmationPage({booking,onBack}:{booking:Booking;onBack:()=>void}){return <><h1>ยืนยันการจอง</h1><section className="card"><h2>ส่งคำขอจองสำเร็จ</h2><p>รหัสการจอง: <b>{booking.bookingCode}</b></p><p>ห้องเรียน: {booking.classroom?.name||booking.classroomId}</p><p>วัตถุประสงค์: {booking.purpose}</p><p>เวลา: {new Date(booking.startAt).toLocaleString()} - {new Date(booking.endAt).toLocaleString()}</p><p>สถานะ: <span className="badge">{booking.status}</span></p><button className="primary" onClick={onBack}>ดูประวัติการจอง</button></section></>}
