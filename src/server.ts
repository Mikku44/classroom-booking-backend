import {app} from './app'; import {env} from './config/env'; import {startBookingReminderWorker} from './services/booking-reminder.service'; app.listen(env.PORT,()=>{console.log(`API listening on ${env.PORT}`);startBookingReminderWorker()});
